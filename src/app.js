(()=>{'use strict';const E=window.MatricaEngine;if(!E)throw new Error('MatricaEngine not loaded');const PROFILE_KEY='matrica.profile.v2',SESSIONS_KEY='matrica.sessions.v2';const state={screen:'welcome',profile:load(PROFILE_KEY,null),question:'',matrix:null,decision:null,activeProtocol:0};const app=document.querySelector('#app'),sheetRoot=document.querySelector('#sheet-root');function load(key,fallback){try{const v=JSON.parse(localStorage.getItem(key));return v??fallback}catch{return fallback}}function save(key,value){try{localStorage.setItem(key,JSON.stringify(value))}catch{}}function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function nl(v=''){return esc(v).replace(/\n/g,'<br>')}function sessions(){const v=load(SESSIONS_KEY,[]);return Array.isArray(v)?v:[]}
function matrixSvg(matrix,cls=''){const nodes=matrix?.display||[{x:50,y:8,value:1,tone:'violet',role:'Личность'},{x:32,y:22,value:12,tone:'neutral',role:'Таланты'},{x:50,y:21,value:11,tone:'violet',role:'Ресурс'},{x:69,y:22,value:22,tone:'neutral',role:'Материя'},{x:17,y:42,value:7,tone:'neutral',role:'Социальное'},{x:31,y:42,value:7,tone:'violet',role:'Действие'},{x:50,y:42,value:6,tone:'center',role:'Центр'},{x:69,y:42,value:8,tone:'cyan',role:'Деньги'},{x:84,y:42,value:8,tone:'cyan',role:'Материя'},{x:31,y:63,value:16,tone:'neutral',role:'Род'},{x:50,y:63,value:22,tone:'cyan',role:'Отношения'},{x:69,y:63,value:18,tone:'gold',role:'Опыт'},{x:50,y:80,value:22,tone:'cyan',role:'Задача'}],edges=[[50,8,17,42],[50,8,84,42],[17,42,50,80],[84,42,50,80],[50,8,50,80],[17,42,84,42],[32,22,69,63],[69,22,31,63],[31,42,69,42],[31,22,50,42],[69,22,50,42],[31,63,50,42],[69,63,50,42],[50,21,31,42],[50,21,69,42],[50,63,31,42],[50,63,69,42]];return `<svg class="matrix-svg ${cls}" viewBox="0 0 100 92" role="img" aria-label="Матрица судьбы"><defs><filter id="glow"><feGaussianBlur stdDeviation="1.2" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter><linearGradient id="edge" x1="0" x2="1"><stop stop-color="#a694ff" stop-opacity=".25"/><stop offset=".5" stop-color="#d7ddff" stop-opacity=".72"/><stop offset="1" stop-color="#7de7ff" stop-opacity=".28"/></linearGradient></defs><circle cx="50" cy="46" r="40" fill="none" stroke="rgba(180,201,255,.18)" stroke-width=".55" stroke-dasharray="1.4 2"/><circle cx="50" cy="46" r="32" fill="none" stroke="rgba(176,151,255,.22)" stroke-width=".45"/><ellipse cx="50" cy="46" rx="43" ry="28" fill="none" stroke="rgba(125,231,255,.17)" stroke-width=".4" transform="rotate(-13 50 46)"/>${edges.map(e=>`<line x1="${e[0]}" y1="${e[1]}" x2="${e[2]}" y2="${e[3]}" stroke="url(#edge)" stroke-width=".55"/>`).join('')}${nodes.map(n=>`<g class="matrix-node matrix-node--${n.tone}" data-arcana="${n.value}" data-role="${esc(n.role)}" transform="translate(${n.x} ${n.y})"><circle r="${n.tone==='center'?6.3:4.6}"/><text y="1.5">${n.value}</text></g>`).join('')}</svg>`}
function shell(content,{back=null,title='',right='',className=''}={}){return `<main class="app-screen ${className}"><div class="statusbar"><span>9:41</span><span class="status-icons"><i></i><i></i><i></i></span></div>${title?`<div class="topline">${back?`<button class="icon-btn" data-nav="${back}" aria-label="Назад">‹</button>`:'<span></span>'}<h2>${title}</h2>${right||'<span></span>'}</div>`:''}${content}<div class="home-indicator"></div></main>`}
function welcome(){return shell(`<div class="welcome-bg" aria-hidden="true"><span></span><span></span><b></b></div><div class="welcome-matrix">${matrixSvg(null,'hero-matrix')}</div><div class="welcome-copy"><h1>Matrica</h1><p class="caps">НАВИГАТОР РЕШЕНИЙ</p><p>Помогаем увидеть скрытые связи,<br>оценить варианты и сделать<br>верный шаг.</p></div><button class="primary-btn welcome-cta" data-nav="question">Начать путь</button><div class="pagination"><b></b><i></i><i></i></div>`,{className:'welcome-screen'})}
function question(){return shell(`<section class="question-copy"><h1>Какой вопрос<br>вас волнует?</h1><p>Сформулируйте коротко и по сути.</p></section><label class="question-card"><textarea id="questionInput" maxlength="300" rows="5" placeholder="Например: открывать ли свой бизнес?">${esc(state.question)}</textarea><span id="counter">${state.question.length} / 300</span></label><div class="tip-card" id="questionTip"><strong>Совет</strong><p>Чем яснее вопрос, тем точнее матрица и рекомендации.</p></div><div class="flow-ribbons" aria-hidden="true"><i></i><i></i><i></i></div><button class="primary-btn continue" data-action="continue-question"><span>Продолжить</span><b>→</b></button>`,{back:'welcome',className:'question-screen'})}
function matrix(){const right=`<button class="info-btn" data-action="matrix-info" aria-label="Как читать матрицу">i</button>`;return shell(`<div class="tabs"><span class="active">Текущая матрица</span><span>${state.profile?.name?esc(state.profile.name):'Профиль'} · ${formatDate(state.profile?.birthDate)}</span></div><div class="matrix-stage">${matrixSvg(state.matrix,'large-matrix')}<div class="orbit-particle p1"></div><div class="orbit-particle p2"></div><div class="orbit-particle p3"></div></div><div class="legend"><span class="v">Дух</span><span class="p">Таланты</span><span class="g">Отношения</span><span class="c">Реализация</span><span class="n">Род</span><span class="w">Материя</span></div>${bottomNav('matrix')}`,{back:'question',title:'Ваша матрица',right,className:'matrix-screen'})}
function analysis(){const steps=state.decision.steps;return shell(`<section class="analysis-heading"><div><h1>Анализ решения</h1><p>7-шаговый протокол</p></div><span>⌘</span></section><div class="protocol-list">${steps.map((s,i)=>`<button class="protocol-row ${i===state.activeProtocol?'active':''}" data-protocol="${i}"><span class="protocol-num">${i+1}</span><span class="protocol-copy"><strong>${esc(s.title)}</strong><small>${esc(i===0?shorten(state.question,36):s.short)}</small></span><span class="chev">›</span></button>`).join('')}</div>${bottomNav('overview')}`,{className:'analysis-screen'})}
function recommendation(){const d=state.decision;return shell(`<div class="result-orbit"><div class="check">✓</div><i></i><i></i></div><section class="result-card"><p>Сейчас для вас разумнее:</p><h3>${nl(d.headline)}</h3><div class="metrics"><span><b>◎</b>Потенциал <em>${esc(d.potential)}</em></span><span><b>♢</b>Риски <em>${esc(d.risk)}</em></span><span><b>◈</b>Ресурсы <em>${esc(d.resources)}</em></span></div><p class="result-note">${esc(d.note)}</p></section><button class="primary-btn result-action" data-action="next-action"><span>Сделать обратимый шаг</span><b>→</b></button><button class="save-btn" data-action="save-session">♡ &nbsp; Сохранить результат</button>`,{back:'analysis',title:'Рекомендация',className:'result-screen'})}
function bottomNav(active){return `<nav class="bottom-nav"><button class="${active==='overview'?'active':''}" data-nav="analysis"><span>⌂</span><small>Обзор</small></button><button class="${active==='matrix'?'active':''}" data-nav="matrix"><span>✧</span><small>Матрица</small></button><button data-action="history"><span>◷</span><small>История</small></button></nav>`}
function render(){document.body.dataset.screen=state.screen;const body=state.screen==='welcome'?welcome():state.screen==='question'?question():state.screen==='matrix'?matrix():state.screen==='analysis'?analysis():recommendation();app.innerHTML=`<div class="cosmos" aria-hidden="true"><i class="s1"></i><i class="s2"></i><i class="s3"></i><svg viewBox="0 0 900 220"><path d="M35 16 C260 15 460 63 606 116 S786 181 820 126"/><path d="M232 38 C411 58 554 100 650 137 S773 175 833 121"/><circle cx="641" cy="137" r="3"/></svg></div><header class="desktop-brand"><div class="brand-mark">M</div><div><strong>Matrica</strong><span>НАВИГАТОР РЕШЕНИЙ</span></div></header><section class="device"><div class="dynamic-island"></div>${body}</section><p class="desktop-hint">Матрица → гипотеза → факты → обратимый шаг</p>`;if(state.screen==='question')requestAnimationFrame(()=>document.querySelector('#questionInput')?.focus())}
function navigate(screen){if(['matrix','analysis','recommendation'].includes(screen)&&!prepareDecision())return;state.screen=screen;render()}
function prepareDecision(){if(state.question.trim().length<10){state.screen='question';render();showQuestionTip('Сделайте вопрос чуть конкретнее','Напишите один текущий выбор хотя бы одним коротким предложением.');return false}if(!state.profile?.birthDate){openProfileSheet();return false}try{state.matrix=E.calculateMatrix(state.profile.birthDate);state.decision=E.buildDecision(state.question.trim(),state.matrix);return true}catch{openProfileSheet('Проверьте дату рождения');return false}}
function showQuestionTip(title,text){const card=document.querySelector('#questionTip');if(card){card.classList.add('attention');card.querySelector('strong').textContent=title;card.querySelector('p').textContent=text}}
function openProfileSheet(title='Данные для вашей матрицы'){const today=new Date().toISOString().slice(0,10);openSheet({eyebrow:'Один раз для расчёта',title,body:`<label class="sheet-field"><span>Имя <small>необязательно</small></span><input id="profileName" autocomplete="name" value="${esc(state.profile?.name||'')}" placeholder="Как к вам обращаться"></label><label class="sheet-field"><span>Дата рождения</span><input id="profileBirth" type="date" max="${today}" value="${esc(state.profile?.birthDate||'')}"></label><p class="privacy-note">Расчёт выполняется прямо в браузере. Дата нужна только для персональной Матрицы и сохраняется на этом устройстве.</p>`,actions:[{id:'profile-save',label:'Построить матрицу',primary:true}]})}
function openSheet({eyebrow='',title='',body='',actions=[]}){sheetRoot.innerHTML=`<div class="sheet open"><button class="sheet-backdrop" data-action="close-sheet" aria-label="Закрыть"></button><section class="sheet-card" role="dialog" aria-modal="true"><div class="sheet-handle"></div><p class="sheet-eyebrow">${esc(eyebrow)}</p><h2>${esc(title)}</h2><div class="sheet-body">${body}</div><div class="sheet-actions">${actions.map(a=>`<button class="sheet-btn ${a.primary?'primary':''}" data-sheet="${a.id}">${esc(a.label)}</button>`).join('')}</div></section></div>`}function closeSheet(){sheetRoot.innerHTML=''}
function protocolSheet(index){state.activeProtocol=index;render();const step=state.decision.steps[index];openSheet({eyebrow:`${index+1} / 7 · ${state.decision.domainTitle}`,title:step.title,body:`<p>${nl(step.body)}</p>`,actions:[...(index>0?[{id:`protocol-${index-1}`,label:'Назад'}]:[]),{id:index<6?`protocol-${index+1}`:'show-result',label:index<6?'Следующий шаг':'Показать рекомендацию',primary:true}]})}
function arcanaSheet(value,role){const a=E.ARCANA[value];openSheet({eyebrow:role,title:`${value} · ${a.name}`,body:`<p><strong>В ресурсе:</strong> ${esc(a.light)}.</p><p><strong>В тени:</strong> ${esc(a.shadow)}.</p><p class="privacy-note">Смотрите на это как на вопрос для наблюдения, а не как на факт о себе.</p>`,actions:[{id:'close',label:'Понятно',primary:true}]})}
function historySheet(){const list=sessions();openSheet({eyebrow:'История',title:list.length?'Сохранённые решения':'История пока пуста',body:list.length?`<div class="history-list">${list.map(s=>`<button class="history-item" data-session="${s.id}"><strong>${esc(s.question)}</strong><span>${esc(s.headline)}</span></button>`).join('')}</div>`:'<p>Сохраните рекомендацию — и сможете вернуться к ней позже.</p>',actions:[{id:'close',label:'Готово',primary:true}]})}
function saveSession(){if(!prepareDecision())return;const list=sessions(),item={id:Date.now(),question:state.question.trim(),headline:state.decision.headline,next:state.decision.next,profile:state.profile,birthDate:state.profile.birthDate};save(SESSIONS_KEY,[item,...list.filter(x=>x.question!==item.question)].slice(0,12));const b=document.querySelector('[data-action="save-session"]');if(b)b.innerHTML='✓ &nbsp; Сохранено'}
function formatDate(v){if(!v)return'';const[y,m,d]=v.split('-');return`${d}.${m}.${y}`}function shorten(v,n){return v.length>n?`${v.slice(0,n-1)}…`:v}
document.addEventListener('input',e=>{if(e.target.id==='questionInput'){state.question=e.target.value;const c=document.querySelector('#counter');if(c)c.textContent=`${state.question.length} / 300`}});
document.addEventListener('click',e=>{const nav=e.target.closest('[data-nav]');if(nav){navigate(nav.dataset.nav);return}const act=e.target.closest('[data-action]');if(act){const id=act.dataset.action;if(id==='continue-question'){state.question=document.querySelector('#questionInput')?.value||state.question;if(state.question.trim().length<10){showQuestionTip('Сделайте вопрос чуть конкретнее','Напишите один текущий выбор хотя бы одним коротким предложением.');return}if(!state.profile?.birthDate){openProfileSheet();return}navigate('matrix');return}if(id==='matrix-info'){openSheet({eyebrow:'Как читать экран',title:'Матрица — не приговор',body:'<p>Нажимайте на числа, чтобы увидеть архетип. В решении используем правило:</p><div class="sheet-fact"><strong>Сигнал → гипотеза → проверка фактами → действие</strong></div>',actions:[{id:'go-analysis',label:'Перейти к анализу',primary:true}]});return}if(id==='history'){historySheet();return}if(id==='next-action'){openSheet({eyebrow:'Минимальный обратимый шаг',title:'Что сделать сейчас',body:`<p>${esc(state.decision.next)}</p><div class="sheet-fact"><span>Что изменит рекомендацию</span><strong>${esc(state.decision.changer)}</strong></div>`,actions:[{id:'save-and-close',label:'Сохранить'},{id:'mark-started',label:'Начать',primary:true}]});return}if(id==='save-session'){saveSession();return}if(id==='close-sheet'){closeSheet();return}}const p=e.target.closest('[data-protocol]');if(p){protocolSheet(Number(p.dataset.protocol));return}const node=e.target.closest('[data-arcana]');if(node&&state.screen==='matrix'){arcanaSheet(Number(node.dataset.arcana),node.dataset.role);return}const sheet=e.target.closest('[data-sheet]');if(sheet){const id=sheet.dataset.sheet;if(id==='profile-save'){const birth=document.querySelector('#profileBirth')?.value||'',name=document.querySelector('#profileName')?.value.trim()||'';if(!E.parseBirthDate(birth)){document.querySelector('#profileBirth')?.classList.add('invalid');return}state.profile={name,birthDate:birth};save(PROFILE_KEY,state.profile);closeSheet();navigate('matrix');return}if(id.startsWith('protocol-')){closeSheet();protocolSheet(Number(id.split('-')[1]));return}if(id==='show-result'){closeSheet();navigate('recommendation');return}if(id==='go-analysis'){closeSheet();navigate('analysis');return}if(id==='close'){closeSheet();return}if(id==='save-and-close'){saveSession();closeSheet();return}if(id==='mark-started'){saveSession();sheet.textContent='Шаг начат ✓';setTimeout(closeSheet,650);return}}const session=e.target.closest('[data-session]');if(session){const item=sessions().find(s=>String(s.id)===session.dataset.session);if(item){state.question=item.question;state.profile=item.profile||state.profile;save(PROFILE_KEY,state.profile);closeSheet();navigate('recommendation')}return}});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeSheet()});render();})();
